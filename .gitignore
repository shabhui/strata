# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller
build/
dist/
*.spec.bak

# 本地工具产物(基准和探针数据是机器相关的,别提交)
# 探针脚本本身要提交 —— 结论怎么来的必须留痕;跑出来的数字不提交,
# 因为它们只在这台机器、这块盘、这个时刻成立。
tools/*_result.txt
tools/*.out

# 编辑器 / 工具链本地配置
.claude/
.vscode/
.idea/

# 排查过程的草稿。结论该落在代码注释和提交说明里,不是单独一份会过期的文档 ——
# 那些数字只在这台机器、这块盘、这个时刻成立,而文档不会跟着代码走。
docs/superpowers/

# 数据库只存在 %LOCALAPPDATA%\Strata,这里兜个底
*.db
*.db-wal
*.db-shm
*.log
