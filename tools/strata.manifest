<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <assemblyIdentity type="win32" name="Strata" version="1.0.0.0" processorArchitecture="amd64"/>

  <!-- 提权要求由 PyInstaller 的 uac_admin=True 合并进来,这里不重复写。 -->

  <application xmlns="urn:schemas-microsoft-com:asm.v3">
    <windowsSettings>
      <!-- 长路径支持。这个工具要遍历整块盘,超过 260 字符的路径很常见
           (node_modules 是重灾区)。不开这个开关,那些路径上的 API 调用
           会失败,扫描结果会莫名少掉一块。

           注意:这只是一半 —— 系统侧还要
           HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled=1
           (Win10 1607+ 默认开)。所以扫描代码不能只靠它,遇到超长路径
           仍要能优雅跳过。 -->
      <longPathAware xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">true</longPathAware>
    </windowsSettings>
  </application>

  <compatibility xmlns="urn:schemas-microsoft-com:compatibility.v1">
    <application>
      <!-- 声明支持到 Win10/11,免得系统套用兼容性垫片 -->
      <supportedOS Id="{8e0f7a12-bfb3-4fe8-b9a5-48fd50a15a9a}"/>
      <supportedOS Id="{1f676c76-80e1-4239-95bb-83d0f6d0da78}"/>
      <supportedOS Id="{4a2f28e3-53b9-4441-ba9c-d69d4a4a6e38}"/>
    </application>
  </compatibility>
</assembly>
